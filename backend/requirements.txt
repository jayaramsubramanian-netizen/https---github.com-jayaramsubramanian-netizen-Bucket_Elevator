# VECTRIX™ Backend — Python 3.14 compatible (tested 3.11–3.14)
# Root cause of original error: pydantic==2.6.4 pinned pydantic-core==2.16.3
# which has no pre-built cp314 wheel and its Rust build fails on Python 3.14
# due to ForwardRef._evaluate() API change.
# Fix: upgrade to pydantic>=2.11 (pydantic-core>=2.28 has cp314 wheels).

fastapi>=0.115.0
uvicorn[standard]>=0.29.0
pydantic>=2.11.0
python-multipart>=0.0.9
reportlab>=3.8.0

# Added 2026-06 for the NSGA-II multi-objective optimizer (vectrix_optimizer_v2.py)
pymoo>=0.6.1